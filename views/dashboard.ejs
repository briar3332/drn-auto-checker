<!DOCTYPE html>
<html>
<head>
    <title>DRN Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-primary bg-gradient text-white">
    <div class="container mt-4">
        <h2 class="text-center">🚗 DRN Auto-Checker Dashboard</h2>
        <p class="text-center">Connected - Auto-checking every 5 minutes</p>
        <div class="text-end">
            <a href="/logout" class="btn btn-light btn-sm">Logout</a>
        </div>
        <div class="card mt-3 p-3">
            <p><strong>Last Check:</strong> <%= lastCheck %></p>
            <div class="d-flex gap-3 justify-content-center">
                <button class="btn btn-primary">Force Check Now</button>
                <button class="btn btn-success">Mark All Processed</button>
                <button class="btn btn-danger">Clear All Data</button>
            </div>
            <div class="row mt-4 text-center">
                <div class="col bg-light text-dark p-3 m-2 rounded">New Emails<br><h3><%= emailStats.new %></h3></div>
                <div class="col bg-light text-dark p-3 m-2 rounded">Total Emails<br><h3><%= emailStats.total %></h3></div>
                <div class="col bg-light text-dark p-3 m-2 rounded">Today's Emails<br><h3><%= emailStats.today %></h3></div>
            </div>
        </div>
    </div>
</body>
</html>
